fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6

# Extractive QA — CPU-only torch keeps the image small (~500MB) vs GPU (~2GB)
# tinyroberta-squad2 model itself is ~82MB, cached on first startup
transformers>=4.44,<5
torch>=2.2,<3
