# =============================================================================
# ML Service Requirements (Aravind — P2-06)
# =============================================================================
# This is for the FastAPI microservice only — NOT the Django backend.
# Install in a separate venv inside the ml_service/ folder:
#   cd ml_service
#   python -m venv venv
#   source venv/bin/activate
#   pip install -r ../requirements/ml_service.txt
# =============================================================================

fastapi>=0.111,<1.0
uvicorn[standard]>=0.29,<1.0
httpx>=0.27,<1.0
numpy>=1.26,<3.0
pandas>=2.2,<3.0
scikit-learn>=1.4,<2.0
# xgboost>=2.0,<3.0               # Uncomment when KAU provides training dataset
# lightgbm>=4.0,<5.0              # Uncomment when KAU provides training dataset
python-decouple>=3.8,<4.0
